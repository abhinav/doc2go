## v0.12.1 - 2026-04-05
### Fixed
- Fixed `-pkg-doc` links for imported types used inside generic type arguments, such as `pkg.Container[pkg.Message]`, so generated docs now link both the outer generic type and the imported type arguments correctly.
